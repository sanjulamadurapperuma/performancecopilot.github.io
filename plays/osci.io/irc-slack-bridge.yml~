---
- name: Slack-IRC Bridge Setup
  hosts: osas
  tasks:
    - name: Create irc-slack user
      user:
        name: irc-slack

    - name: use NPM to install
      become_user: irc-slack
      shell:
